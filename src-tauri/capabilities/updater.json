{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Checking for, downloading and applying an update, and the restart that finishes it.",
  "comment": "Separate from `default` because of `platforms`: the updater and process plugins are not compiled on Android or iOS (see Cargo.toml), and a capability naming permissions from a plugin that isn't registered fails at startup rather than being ignored.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": ["updater:default", "process:allow-restart"]
}
