<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	  Under the hardened runtime (which signed and notarised builds use), the
	  Info.plist usage string alone isn't enough — the process also needs the
	  matching entitlement or the microphone request is denied before the user
	  ever sees a prompt.
	-->
	<key>com.apple.security.device.audio-input</key>
	<true/>
	<key>com.apple.security.network.client</key>
	<true/>
</dict>
</plist>
